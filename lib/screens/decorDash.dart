import 'package:flutter/material.dart';

class Decordash extends StatelessWidget {
  const Decordash({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        automaticallyImplyLeading: true,
      ),
      body: Center(child: Text('Decor Dash')),
    );
  }
}
